int main()
{
      char ch;
    scanf("%c",&ch);
    char str[50];
   scanf("%s",str);
   int i; 
   for(i=0;str[i]!='\0';i++)
{
          if(str[i]==ch) 
          str[i]=str[i]-32;
         
          
   }
    printf("%s",str);

    return 0;
}
input : aditya
 output : AdityA
